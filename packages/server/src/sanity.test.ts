import { describe, expect, it } from "vitest";

// Placeholder test. Its only job is to prove that the test runner, the
// TypeScript setup, and the CI pipeline are all wired together correctly.
// Delete this once real server logic (and real tests) exist.
describe("test pipeline", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
